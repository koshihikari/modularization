◎かな自動入力のプラグイン、もともとのnameがかぶっていた場合使用不可（IEのみ）。

◎読んだだけで動いてしまうプラグインをアクティベートで読むようにする。

◎phpをフォルダごとに読み込みにする。
　また、pcはpc、スマホはスマホのフォルダを読む。
　また、別途Fileから呼び出すなど。

◎classは統一し、アクティベーションにより判断できるようにする。

◎IEで送信してから戻ると、残り項目がうまく表示されない。